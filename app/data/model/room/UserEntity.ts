/**
 * Local SQLite row shape (mirrors ai-project-android's
 * data/model/room/UserEntity.java — a placeholder entity with a
 * single column, not yet wired into any real feature).
 */
export interface UserEntity {
  userId: number
}
