"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.wrapToWidth = wrapToWidth;
exports.createMarkdownRenderState = createMarkdownRenderState;
exports.renderMarkdownLine = renderMarkdownLine;
exports.renderInlineMarkdown = renderInlineMarkdown;
const tslib_1 = require("tslib");
const chalk_1 = tslib_1.__importDefault(require("chalk"));
const wrap_ansi_1 = tslib_1.__importDefault(require("wrap-ansi"));
function wrapToWidth(text, width) {
    if (width < 10) {
        return [text];
    }
    return (0, wrap_ansi_1.default)(text, width, { hard: true, trim: false }).split('\n');
}
function createMarkdownRenderState() {
    return { inCodeBlock: false };
}
const FENCE_REGEX = /^\s*```/;
const BULLET_REGEX = /^(\s*)[-*+]\s+(.*)$/;
const ORDERED_REGEX = /^(\s*)(\d+)\.\s+(.*)$/;
const TERMINAL_CONTROL_CHARACTERS_REGEX = /[\u0000-\u0008\u000b-\u001f\u007f-\u009f]/g;
function renderMarkdownLine(line, state) {
    const sanitizedLine = line.replace(TERMINAL_CONTROL_CHARACTERS_REGEX, '');
    if (FENCE_REGEX.test(sanitizedLine)) {
        state.inCodeBlock = !state.inCodeBlock;
        return null;
    }
    if (state.inCodeBlock) {
        return chalk_1.default.gray(sanitizedLine);
    }
    const bullet = sanitizedLine.match(BULLET_REGEX);
    if (bullet) {
        return `${bullet[1]}${chalk_1.default.dim('•')} ${renderInlineMarkdown(bullet[2])}`;
    }
    const ordered = sanitizedLine.match(ORDERED_REGEX);
    if (ordered) {
        return `${ordered[1]}${chalk_1.default.dim(`${ordered[2]}.`)} ${renderInlineMarkdown(ordered[3])}`;
    }
    return renderInlineMarkdown(sanitizedLine);
}
function renderInlineMarkdown(text) {
    return text
        .replace(/`([^`]+)`/g, (_match, code) => chalk_1.default.cyan(code))
        .replace(/\[([^\]]+)\]\(([^)]+)\)/g, (_match, label, url) => `${chalk_1.default.underline(label)} ${chalk_1.default.dim(`(${url})`)}`)
        .replace(/(\*\*|__)(.+?)\1/g, (_match, _marker, content) => chalk_1.default.bold(content));
}
