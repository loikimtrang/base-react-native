export type ChatReplInput = {
    askAsync(prompt: string): Promise<string | null>;
    close(): void;
};
export declare function createChatReplInput(options?: {
    input?: NodeJS.ReadableStream;
    output?: NodeJS.WritableStream;
    terminal?: boolean;
    history?: string[];
}): ChatReplInput;
