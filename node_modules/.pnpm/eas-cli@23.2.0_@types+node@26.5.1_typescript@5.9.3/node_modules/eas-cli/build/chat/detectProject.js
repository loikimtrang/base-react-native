"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.detectCurrentProjectAsync = detectCurrentProjectAsync;
const config_1 = require("@expo/config");
const findProjectDirAndVerifyProjectSetupAsync_1 = require("../commandUtils/context/contextUtils/findProjectDirAndVerifyProjectSetupAsync");
const AppQuery_1 = require("../graphql/queries/AppQuery");
const expoConfig_1 = require("../project/expoConfig");
async function detectCurrentProjectAsync(graphqlClient) {
    let projectDir;
    try {
        projectDir = await (0, findProjectDirAndVerifyProjectSetupAsync_1.findProjectDirAndVerifyProjectSetupAsync)();
    }
    catch {
        return null;
    }
    if (!projectDir) {
        return null;
    }
    const configPaths = (0, config_1.getConfigFilePaths)(projectDir);
    if (!configPaths.staticConfigPath && !configPaths.dynamicConfigPath) {
        return null;
    }
    let projectId;
    try {
        const expoConfig = await (0, expoConfig_1.getPrivateExpoConfigAsync)(projectDir);
        projectId = expoConfig.extra?.eas?.projectId;
    }
    catch {
        return null;
    }
    if (typeof projectId !== 'string' || !projectId) {
        return null;
    }
    try {
        const app = await AppQuery_1.AppQuery.byIdAsync(graphqlClient, projectId);
        return { accountName: app.ownerAccount.name, label: app.fullName };
    }
    catch {
        return null;
    }
}
