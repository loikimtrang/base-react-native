type ChatMessagePart = {
    type: 'text';
    text: string;
} | {
    type: 'step-start';
} | {
    type: `tool-${string}`;
    toolCallId: string;
    state: 'input-streaming' | 'input-available' | 'output-available' | 'output-error';
    input: unknown;
    output?: unknown;
    errorText?: string;
};
export type ChatMessage = {
    id: string;
    role: 'system' | 'user' | 'assistant';
    parts: ChatMessagePart[];
};
export declare function makeUserMessage(text: string): ChatMessage;
export type ChatToolCall = {
    toolName: string;
    input?: unknown;
    output?: unknown;
    errorText?: string;
};
export type ChatResult = {
    text: string;
    toolCalls: ChatToolCall[];
    assistantMessage: ChatMessage;
};
export declare function streamChatResponseAsync({ messages, accountName, sessionSecret, stream, }: {
    messages: ChatMessage[];
    accountName: string | undefined;
    sessionSecret: string;
    stream: boolean;
}): Promise<ChatResult>;
export {};
