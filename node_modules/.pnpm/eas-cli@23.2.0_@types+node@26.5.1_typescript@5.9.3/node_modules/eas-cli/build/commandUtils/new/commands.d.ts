import { PackageManager } from '../../onboarding/installDependencies';
export declare const TEMPLATE_PACKAGE_NAME = "expo-template-default";
export interface TemplateInfo {
    npmTag: string;
    version: string;
    tarballUrl: string;
}
export interface TemplatePackument {
    'dist-tags'?: Record<string, string>;
    versions?: Record<string, {
        dist: {
            tarball: string;
        };
    }>;
}
export declare function fetchTemplatePackumentAsync(): Promise<TemplatePackument>;
export declare function downloadTemplateAsync(targetProjectDir: string, template: TemplateInfo): Promise<string>;
export declare function installProjectDependenciesAsync(projectDir: string, packageManager: PackageManager): Promise<void>;
export declare function initializeGitRepositoryAsync(projectDir: string): Promise<void>;
