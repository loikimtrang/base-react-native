import { TemplateInfo } from './commands';
export interface SdkVersions {
    latest: number;
    expoGoCompatible: number | null;
    available: number[];
}
/**
 * SDKs without a `releaseNoteUrl` are canary/in-development and are excluded.
 */
export declare function fetchSdkVersionsAsync(): Promise<SdkVersions | null>;
export declare function promptForSdkVersionAsync({ latest, expoGoCompatible, available, }: SdkVersions): Promise<number>;
/**
 * `sdkVersion` accepts "57", "sdk-57", or "latest".
 */
export declare function resolveTemplateAsync({ sdkVersion, }: {
    sdkVersion?: string;
}): Promise<TemplateInfo>;
