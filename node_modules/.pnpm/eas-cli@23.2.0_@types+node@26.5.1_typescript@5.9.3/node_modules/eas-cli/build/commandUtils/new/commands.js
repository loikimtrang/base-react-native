"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TEMPLATE_PACKAGE_NAME = void 0;
exports.fetchTemplatePackumentAsync = fetchTemplatePackumentAsync;
exports.downloadTemplateAsync = downloadTemplateAsync;
exports.installProjectDependenciesAsync = installProjectDependenciesAsync;
exports.initializeGitRepositoryAsync = initializeGitRepositoryAsync;
const tslib_1 = require("tslib");
const chalk_1 = tslib_1.__importDefault(require("chalk"));
const fast_glob_1 = tslib_1.__importDefault(require("fast-glob"));
const fs_extra_1 = tslib_1.__importDefault(require("fs-extra"));
const path_1 = tslib_1.__importDefault(require("path"));
const promises_1 = require("stream/promises");
const tar_1 = require("tar");
const utils_1 = require("./utils");
const fetch_1 = tslib_1.__importDefault(require("../../fetch"));
const installDependencies_1 = require("../../onboarding/installDependencies");
const runCommand_1 = require("../../onboarding/runCommand");
const ora_1 = require("../../ora");
const expoCli_1 = require("../../utils/expoCli");
exports.TEMPLATE_PACKAGE_NAME = 'expo-template-default';
const NPM_REGISTRY_URL = process.env.npm_config_registry?.replace(/\/$/, '') ?? 'https://registry.npmjs.org';
async function fetchTemplatePackumentAsync() {
    const response = await (0, fetch_1.default)(`${NPM_REGISTRY_URL}/${exports.TEMPLATE_PACKAGE_NAME}`, {
        headers: { accept: 'application/vnd.npm.install-v1+json' },
        signal: AbortSignal.timeout(10000),
    });
    return (await response.json());
}
async function downloadTemplateAsync(targetProjectDir, template) {
    const spinner = (0, ora_1.ora)(`${chalk_1.default.bold(`Downloading the project template to ${(0, utils_1.printDirectory)(targetProjectDir)}`)}`).start();
    const targetExisted = await fs_extra_1.default.pathExists(targetProjectDir);
    try {
        // The directory was verified to not exist earlier, but it may have been
        // created while the user was answering prompts.
        if (targetExisted && (await fs_extra_1.default.readdir(targetProjectDir)).length > 0) {
            throw new Error(`Directory ${(0, utils_1.printDirectory)(targetProjectDir)} already exists and is not empty.`);
        }
        await fs_extra_1.default.mkdirp(targetProjectDir);
        const tarballResponse = await (0, fetch_1.default)(template.tarballUrl);
        await (0, promises_1.pipeline)(tarballResponse.body, (0, tar_1.extract)({ cwd: targetProjectDir, strip: 1 }));
        await restoreTemplateDotfilesAsync(targetProjectDir);
        spinner.succeed(`Downloaded ${chalk_1.default.bold(`${exports.TEMPLATE_PACKAGE_NAME}@${template.version}`)} to ${(0, utils_1.printDirectory)(targetProjectDir)}`);
    }
    catch (error) {
        spinner.fail();
        if (!targetExisted) {
            await fs_extra_1.default.remove(targetProjectDir);
        }
        throw error;
    }
    return targetProjectDir;
}
/**
 * npm strips `.gitignore` files from published packages, and the template ships
 * dot-directories with an underscore prefix (e.g. `_vscode`), so restore the
 * real names after extraction. This mirrors the renames in create-expo-app;
 * keep the directory list in sync with SUPPORTED_DIRECTORIES in
 * https://github.com/expo/expo/blob/main/packages/create-expo-app/src/createFileTransform.ts
 */
async function restoreTemplateDotfilesAsync(projectDir) {
    const entries = await (0, fast_glob_1.default)('**/{gitignore,_eas,_vscode,_github,_cursor}', {
        cwd: projectDir,
        onlyFiles: false,
        dot: true,
    });
    // Rename deeper entries first so parent renames do not invalidate child paths.
    entries.sort((a, b) => b.split('/').length - a.split('/').length);
    for (const entry of entries) {
        const basename = path_1.default.basename(entry);
        const dotName = basename === 'gitignore' ? '.gitignore' : `.${basename.slice(1)}`;
        await fs_extra_1.default.move(path_1.default.join(projectDir, entry), path_1.default.join(projectDir, path_1.default.dirname(entry), dotName), { overwrite: true });
    }
}
async function installProjectDependenciesAsync(projectDir, packageManager) {
    const spinner = (0, ora_1.ora)(`${chalk_1.default.bold('Installing project dependencies')}`).start();
    await (0, installDependencies_1.installDependenciesAsync)({
        outputLevel: 'none',
        projectDir,
        packageManager,
    });
    const dependencies = ['expo-updates', '@expo/metro-runtime'];
    spinner.text = `Installing ${dependencies.map(dep => chalk_1.default.bold(dep)).join(', ')}`;
    await (0, expoCli_1.expoCommandAsync)(projectDir, ['install', ...dependencies], { silent: true });
    spinner.succeed(`Installed project dependencies`);
}
async function initializeGitRepositoryAsync(projectDir) {
    const spinner = (0, ora_1.ora)(`${chalk_1.default.bold('Initializing Git repository')}`).start();
    await fs_extra_1.default.remove(path_1.default.join(projectDir, '.git'));
    const commands = [['init'], ['add', '.'], ['commit', '-m', 'Initial commit']];
    for (const args of commands) {
        await (0, runCommand_1.runCommandAsync)({
            cwd: projectDir,
            command: 'git',
            args,
            showOutput: false,
            showSpinner: false,
        });
    }
    spinner.succeed(`Initialized Git repository`);
}
