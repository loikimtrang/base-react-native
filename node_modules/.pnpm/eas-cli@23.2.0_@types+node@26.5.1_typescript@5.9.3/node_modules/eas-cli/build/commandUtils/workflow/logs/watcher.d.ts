import { WorkflowJobResult, WorkflowLogs, WorkflowRawLogLine } from '../types';
import { RealtimeLogsTargetInput } from '../../../graphql/generated';
import { RealtimeLogsClient } from '../../../utils/centrifuge';
import { ExpoGraphqlClient } from '../../context/contextUtils/createGraphqlClient';
export declare class WorkflowJobLogsState {
    private fileLogIds;
    private realtimeLogLines;
    private realtimeLogIds;
    private haveFileLogsCaughtUp;
    private realtimeLogsRevealed;
    private groupedLogs;
    ingestFileLogLines(logLines: WorkflowRawLogLine[]): void;
    ingestRealtimeLogLines(data: unknown): boolean;
    markCompleted(): void;
    getLogs(): WorkflowLogs;
    private revealRealtimeLogs;
}
export declare function realtimeLogsTargetForJob(job: WorkflowJobResult): RealtimeLogsTargetInput | null;
export declare class WorkflowRunLogsWatcher {
    private readonly graphqlClient;
    private readonly createRealtimeLogsClient;
    private readonly onRealtimeLogs;
    private readonly trackedJobs;
    private realtimeLogsClient?;
    constructor(graphqlClient: ExpoGraphqlClient, createRealtimeLogsClient: () => RealtimeLogsClient | null, onRealtimeLogs: () => void);
    syncJobsAsync(jobs: WorkflowJobResult[]): Promise<Map<string, WorkflowJobLogsState>>;
    close(): void;
    private syncJobAsync;
    private fetchLogsAsync;
    private getRealtimeLogsClient;
    private subscribeAsync;
}
