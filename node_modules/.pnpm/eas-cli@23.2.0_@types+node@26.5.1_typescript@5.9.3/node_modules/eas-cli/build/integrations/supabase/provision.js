"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SUPABASE_REGION_CHOICES = exports.PROVISION_RECEIPT_MAX_CONSECUTIVE_FETCH_ERRORS = exports.PROVISION_RECEIPT_MAX_CHECKS = void 0;
exports.projectNameSuffixForEnvironments = projectNameSuffixForEnvironments;
exports.toProvisionPollError = toProvisionPollError;
exports.additionalProvisionFailureHint = additionalProvisionFailureHint;
exports.primaryProvisionFailureHint = primaryProvisionFailureHint;
exports.pollProvisionReceiptAsync = pollProvisionReceiptAsync;
exports.authorizeViaBrowserAsync = authorizeViaBrowserAsync;
exports.loadOrganizationsBestEffortAsync = loadOrganizationsBestEffortAsync;
exports.pollForConnectionAsync = pollForConnectionAsync;
exports.resolvePublishableKeyAsync = resolvePublishableKeyAsync;
exports.resolveRegionAsync = resolveRegionAsync;
exports.resolveOrganizationAsync = resolveOrganizationAsync;
const tslib_1 = require("tslib");
const better_opn_1 = tslib_1.__importDefault(require("better-opn"));
const chalk_1 = tslib_1.__importDefault(require("chalk"));
const supabase_1 = require("../../commandUtils/supabase");
const client_1 = require("../../graphql/client");
const SupabaseMutation_1 = require("../../graphql/mutations/SupabaseMutation");
const SupabaseQuery_1 = require("../../graphql/queries/SupabaseQuery");
const log_1 = tslib_1.__importStar(require("../../log"));
const ora_1 = require("../../ora");
const prompts_1 = require("../../prompts");
const promise_1 = require("../../utils/promise");
const pollForBackgroundJobReceiptAsync_1 = require("../../utils/pollForBackgroundJobReceiptAsync");
// The server holds the pending OAuth row for 15 minutes; match that so we don't time out on an
// approval the user completes within the window.
const CONNECTION_POLL_INTERVAL_MS = 2_000;
const CONNECTION_POLL_TIMEOUT_MS = 15 * 60 * 1_000;
const MAX_CONSECUTIVE_CONNECTION_ERRORS = 10;
// A freshly provisioned project takes a minute or two to become healthy; the publishable key only
// resolves once it is.
const READINESS_POLL_INTERVAL_MS = 3_000;
const READINESS_POLL_TIMEOUT_MS = 5 * 60 * 1_000;
// A server fault is worth retrying, but not for the full timeout: once it repeats this many
// times it isn't a provisioning delay, and the user shouldn't wait five minutes to hear it.
const MAX_CONSECUTIVE_READINESS_ERRORS = 10;
// Background job create + publishable-key polling can take 5+ minutes; allow 7 min at 1s interval.
exports.PROVISION_RECEIPT_MAX_CHECKS = 420;
exports.PROVISION_RECEIPT_MAX_CONSECUTIVE_FETCH_ERRORS = 3;
exports.SUPABASE_REGION_CHOICES = [
    { title: 'Americas (US)', value: 'americas' },
    { title: 'Europe / Middle East / Africa', value: 'emea' },
    { title: 'Asia Pacific', value: 'apac' },
];
function projectNameSuffixForEnvironments(environments) {
    return [...environments]
        .sort()
        .join('-')
        .replace(/[^a-zA-Z0-9._-]+/g, '-')
        .slice(0, 32);
}
function toProvisionPollError(error, { hint }) {
    if (!(error instanceof pollForBackgroundJobReceiptAsync_1.BackgroundJobReceiptPollError)) {
        return error instanceof Error ? error : new Error(String(error));
    }
    const trimmedHint = hint.trim();
    const join = (message) => trimmedHint ? `${message.trimEnd()}\n\n${trimmedHint}` : message;
    if (error.errorData.errorType === pollForBackgroundJobReceiptAsync_1.BackgroundJobReceiptPollErrorType.JOB_FAILED_NO_WILL_RETRY) {
        return new Error(join(error.errorData.receiptErrorMessage ?? error.message));
    }
    if (error.errorData.errorType === pollForBackgroundJobReceiptAsync_1.BackgroundJobReceiptPollErrorType.TIMEOUT ||
        error.errorData.errorType === pollForBackgroundJobReceiptAsync_1.BackgroundJobReceiptPollErrorType.NULL_RECEIPT) {
        return new Error(join('Timed out or lost contact while waiting for Supabase project provision.'));
    }
    return error;
}
/** Fast-forward guidance when additional (--environment) provision fails permanently. */
function additionalProvisionFailureHint(environments) {
    return [
        'Free a slot on your Supabase plan (delete, pause, or upgrade a project), then re-run:',
        `       eas integrations:supabase:connect --environment ${environments.join(',')}`,
        'Or set EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY on those EAS environments to an existing Supabase project.',
    ].join('\n');
}
/** Where to find a project for --link (primary connect path). */
function primaryProvisionFailureHint() {
    return [
        'If a project already exists in your Supabase dashboard, link it instead of provisioning:',
        '       eas integrations:supabase:connect --link <project-url>',
    ].join('\n');
}
async function pollProvisionReceiptAsync(graphqlClient, receipt, { startMessage, waitingMessage, failureMessage, failureHint, }) {
    const spinner = (0, ora_1.ora)(startMessage).start();
    try {
        spinner.text = waitingMessage;
        const finalized = await (0, pollForBackgroundJobReceiptAsync_1.pollForBackgroundJobReceiptAsync)(graphqlClient, receipt, {
            maxChecks: exports.PROVISION_RECEIPT_MAX_CHECKS,
            maxConsecutiveFetchErrors: exports.PROVISION_RECEIPT_MAX_CONSECUTIVE_FETCH_ERRORS,
        });
        if (!finalized) {
            throw new Error('Supabase project provision finished without a receipt.');
        }
        // Stopped, not succeeded: the caller resolves what was provisioned and owns that message.
        spinner.stop();
        return finalized;
    }
    catch (error) {
        spinner.fail(failureMessage);
        throw toProvisionPollError(error, { hint: failureHint });
    }
}
async function authorizeViaBrowserAsync(graphqlClient, account, nonInteractive) {
    if (nonInteractive) {
        throw new Error(`Connecting Supabase requires approving access in a browser, which isn't possible in non-interactive mode. Re-run \`eas integrations:supabase:connect\` interactively.`);
    }
    const { url } = await SupabaseMutation_1.SupabaseMutation.beginSupabaseOAuthAsync(graphqlClient, {
        accountId: account.id,
    });
    log_1.default.addNewLineIfNone();
    log_1.default.log(`Authorize Expo to access your Supabase account in the browser. You'll need an existing Supabase account.`);
    log_1.default.log(`If your browser doesn't open automatically, authorize at this link: ${(0, log_1.link)(url)}`);
    void (0, better_opn_1.default)(url).catch(() => false);
    const spinner = (0, ora_1.ora)('Waiting for you to authorize in Supabase (up to 15 minutes; press Ctrl-C to cancel)').start();
    try {
        const connection = await pollForConnectionAsync(graphqlClient, account.id);
        const organizations = await loadOrganizationsBestEffortAsync(graphqlClient, account.id);
        spinner.succeed(`Connected Supabase organization ${chalk_1.default.bold((0, supabase_1.formatSupabaseOrganization)(connection, organizations ?? undefined))}`);
        return connection;
    }
    catch (error) {
        spinner.fail("Couldn't confirm the Supabase connection");
        throw error;
    }
}
async function loadOrganizationsBestEffortAsync(graphqlClient, accountId) {
    try {
        return await SupabaseMutation_1.SupabaseMutation.listSupabaseOrganizationsAsync(graphqlClient, accountId);
    }
    catch {
        return null;
    }
}
async function pollForConnectionAsync(graphqlClient, accountId) {
    const deadline = Date.now() + CONNECTION_POLL_TIMEOUT_MS;
    let consecutiveErrors = 0;
    for (;;) {
        let connection = null;
        try {
            connection = await SupabaseQuery_1.SupabaseQuery.getSupabaseConnectionByAccountIdAsync(graphqlClient, accountId, { useCache: false });
            consecutiveErrors = 0;
        }
        catch (error) {
            consecutiveErrors += 1;
            log_1.default.debug(`Polling for the Supabase connection failed: ${error}`);
            if ((0, client_1.isPermanentGraphqlError)(error) ||
                consecutiveErrors >= MAX_CONSECUTIVE_CONNECTION_ERRORS) {
                log_1.default.error('Gave up checking whether the Supabase authorization finished. Fix the error below, then re-run `eas integrations:supabase:connect` — an authorization you already approved is still picked up.');
                throw error;
            }
        }
        if (connection) {
            return connection;
        }
        if (Date.now() >= deadline) {
            throw new Error('Timed out waiting for the Supabase connection. If you authorized it in your browser, re-run `eas integrations:supabase:connect` — it will pick up the connection.');
        }
        await (0, promise_1.sleepAsync)(CONNECTION_POLL_INTERVAL_MS);
    }
}
async function resolvePublishableKeyAsync(graphqlClient, appId, project) {
    const spinner = (0, ora_1.ora)('Waiting for the Supabase project to finish provisioning').start();
    const deadline = Date.now() + READINESS_POLL_TIMEOUT_MS;
    // The server returns a null key while the project is still provisioning, and throws for a real
    // problem. A rejected request can't succeed on a retry; a server fault might, but not forever.
    let consecutiveErrors = 0;
    for (;;) {
        let key = null;
        try {
            key = await SupabaseMutation_1.SupabaseMutation.fetchSupabasePublishableKeyAsync(graphqlClient, appId);
            consecutiveErrors = 0;
        }
        catch (error) {
            consecutiveErrors += 1;
            log_1.default.debug(`Polling for the Supabase project readiness failed: ${error}`);
            if ((0, client_1.isPermanentGraphqlError)(error) || consecutiveErrors >= MAX_CONSECUTIVE_READINESS_ERRORS) {
                spinner.fail("Couldn't reach the Supabase project");
                log_1.default.error(`The project may exist even though EAS can't read its key. Check it at ${(0, supabase_1.getSupabaseProjectDashboardUrl)(project)} before you re-run \`eas integrations:supabase:connect\`.`);
                throw error;
            }
        }
        if (key) {
            spinner.succeed('Supabase project is ready');
            return key;
        }
        if (Date.now() >= deadline) {
            spinner.fail('Supabase project did not finish provisioning in time');
            throw new Error(`The Supabase project is still provisioning. Once it's healthy (check ${(0, supabase_1.getSupabaseProjectDashboardUrl)(project)}), re-run \`eas integrations:supabase:connect\` to finish writing the environment variables.`);
        }
        await (0, promise_1.sleepAsync)(READINESS_POLL_INTERVAL_MS);
    }
}
async function resolveRegionAsync(flagValue, nonInteractive) {
    if (flagValue !== undefined) {
        // The server accepts both the smart-group values (americas | emea | apac) and raw region
        // codes (e.g. us-east-1), so any non-empty value passes through.
        const region = flagValue.trim();
        if (!region) {
            throw new Error('Pass a Supabase region to --region (americas, emea, apac, or a raw code like us-east-1).');
        }
        return region;
    }
    if (nonInteractive) {
        throw new Error('A Supabase region is required in non-interactive mode. Pass --region (americas, emea, or apac). The region is permanent once the project is created.');
    }
    return await (0, prompts_1.selectAsync)('Select a Supabase region (permanent once the project is created)', exports.SUPABASE_REGION_CHOICES);
}
async function resolveOrganizationAsync(graphqlClient, accountId, connection, organizationFlag, nonInteractive, preloadedOrganizations) {
    if (organizationFlag) {
        if (organizationFlag === connection.supabaseOrganizationSlug) {
            return connection;
        }
        const organizations = preloadedOrganizations ??
            (await SupabaseMutation_1.SupabaseMutation.listSupabaseOrganizationsAsync(graphqlClient, accountId));
        if (!organizations.some(organization => organization.slug === organizationFlag)) {
            throw new Error(`Supabase organization ${chalk_1.default.bold(organizationFlag)} isn't one of your connected organizations (${organizations
                .map(organization => organization.slug)
                .join(', ')}).`);
        }
        return await SupabaseMutation_1.SupabaseMutation.setSupabaseConnectionOrganizationAsync(graphqlClient, {
            supabaseConnectionId: connection.id,
            organizationSlug: organizationFlag,
        });
    }
    if (nonInteractive) {
        return connection;
    }
    const organizations = preloadedOrganizations ??
        (await SupabaseMutation_1.SupabaseMutation.listSupabaseOrganizationsAsync(graphqlClient, accountId));
    if (organizations.length <= 1) {
        return connection;
    }
    const chosen = await (0, prompts_1.selectAsync)('Select the Supabase organization to use', organizations.map(organization => ({
        title: `${organization.name} (${organization.slug})`,
        value: organization.slug,
    })));
    if (chosen === connection.supabaseOrganizationSlug) {
        return connection;
    }
    return await SupabaseMutation_1.SupabaseMutation.setSupabaseConnectionOrganizationAsync(graphqlClient, {
        supabaseConnectionId: connection.id,
        organizationSlug: chosen,
    });
}
