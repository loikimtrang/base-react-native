"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EAS_SUPABASE_ENVIRONMENTS = exports.SUPABASE_ENV_LABEL = exports.EAS_SUPABASE_PUBLISHABLE_KEY_ENV_VAR_NAME = exports.EAS_SUPABASE_URL_ENV_VAR_NAME = void 0;
exports.supabaseMoveConfirmMessage = supabaseMoveConfirmMessage;
exports.supabaseEnvironmentCancelMessage = supabaseEnvironmentCancelMessage;
exports.createSupabaseEnvVars = createSupabaseEnvVars;
exports.confirmOverwriteForAdditionalProjectAsync = confirmOverwriteForAdditionalProjectAsync;
const environment_1 = require("../../build/utils/environment");
const variables_1 = require("../../environments/variables");
const generated_1 = require("../../graphql/generated");
const prompts_1 = require("../../prompts");
exports.EAS_SUPABASE_URL_ENV_VAR_NAME = 'EXPO_PUBLIC_SUPABASE_URL';
exports.EAS_SUPABASE_PUBLISHABLE_KEY_ENV_VAR_NAME = 'EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY';
/** Label passed into shared env helpers for prompts and log lines. */
exports.SUPABASE_ENV_LABEL = 'Supabase';
// Production-first, matching the PostHog and Convex integrations.
exports.EAS_SUPABASE_ENVIRONMENTS = [
    environment_1.DefaultEnvironment.Production,
    environment_1.DefaultEnvironment.Preview,
    environment_1.DefaultEnvironment.Development,
];
function supabaseMoveConfirmMessage(variableName, overlappingEnvironments) {
    return `Move ${variableName} for ${overlappingEnvironments} to the additional Supabase project?`;
}
function supabaseEnvironmentCancelMessage(knownEnvironments) {
    return `Canceled. No additional Supabase project was provisioned. Create the environment(s) first, or pass only existing ones (known: ${knownEnvironments}).`;
}
function createSupabaseEnvVars(url, publishableKey) {
    return [
        {
            name: exports.EAS_SUPABASE_URL_ENV_VAR_NAME,
            value: url,
            visibility: generated_1.EnvironmentVariableVisibility.Public,
        },
        {
            name: exports.EAS_SUPABASE_PUBLISHABLE_KEY_ENV_VAR_NAME,
            value: publishableKey,
            visibility: generated_1.EnvironmentVariableVisibility.Public,
        },
    ];
}
async function confirmOverwriteForAdditionalProjectAsync(graphqlClient, projectId, environments, nonInteractive, overwrite) {
    if (overwrite) {
        return { forceOverwrite: true };
    }
    const names = [exports.EAS_SUPABASE_URL_ENV_VAR_NAME, exports.EAS_SUPABASE_PUBLISHABLE_KEY_ENV_VAR_NAME];
    const targetSet = new Set(environments);
    for (const name of names) {
        const existingVariables = await (0, variables_1.loadProjectScopedEnvVarsAsync)(graphqlClient, projectId, name);
        const hasOverlap = existingVariables.some(variable => (variable.environments ?? []).some(environment => targetSet.has(environment)));
        if (!hasOverlap) {
            continue;
        }
        if (nonInteractive) {
            throw new Error(`EAS already has ${name} for ${environments.join(', ')}. Re-run with --overwrite to replace it before provisioning an additional project.`);
        }
        const proceed = await (0, prompts_1.confirmAsync)({
            message: `EAS already has ${name} covering ${environments.join(', ')}. Continue and move those values to the additional Supabase project?`,
        });
        if (!proceed) {
            throw new Error(`Canceled. No additional Supabase project was provisioned. Pass --overwrite to replace ${name} for ${environments.join(', ')}, or leave those environments on the primary project.`);
        }
        // One confirm covers both vars; stop asking. Force overwrite so the per-var upsert
        // doesn't prompt again and can't return false after we already billed the project.
        return { forceOverwrite: true };
    }
    return { forceOverwrite: false };
}
