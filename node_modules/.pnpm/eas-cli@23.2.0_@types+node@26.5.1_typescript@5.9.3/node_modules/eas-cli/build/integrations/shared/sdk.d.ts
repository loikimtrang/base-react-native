import { ExpoConfig } from '@expo/config';
export type SdkInstallResult = {
    status: 'installed';
    dynamicConfigGuidance?: string;
} | {
    status: 'failed';
};
export declare function getSpawnErrorOutput(error: unknown): string;
export declare function extractDynamicConfigGuidance(output: string): string | null;
export declare function envForExpoInstall(): NodeJS.ProcessEnv;
export declare function installSdkPackagesAsync(projectDir: string, { packages, label, jsonFlag }: {
    packages: string[];
    label: string;
    jsonFlag: boolean;
}): Promise<SdkInstallResult>;
export declare function addConfigPluginAsync(projectDir: string, exp: ExpoConfig, { plugin }: {
    plugin: string;
}): Promise<string | null>;
export declare function setupSdkAndConfigAsync(projectDir: string, exp: ExpoConfig, { packages, plugin, label, jsonFlag, }: {
    packages: string[];
    plugin: string;
    label: string;
    jsonFlag: boolean;
}): Promise<string[]>;
