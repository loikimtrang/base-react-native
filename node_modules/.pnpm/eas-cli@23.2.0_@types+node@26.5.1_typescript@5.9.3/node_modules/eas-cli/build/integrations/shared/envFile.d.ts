import { EnvVar } from '../../environments/variables';
export declare function writeEnvLocalAsync(projectDir: string, envVars: EnvVar[], { label, nonInteractive, overwrite, }: {
    label: string;
    nonInteractive: boolean;
    overwrite: boolean;
}): Promise<boolean>;
export declare function mergeEnvContent(rawContent: string, newVars: Record<string, string>): string;
