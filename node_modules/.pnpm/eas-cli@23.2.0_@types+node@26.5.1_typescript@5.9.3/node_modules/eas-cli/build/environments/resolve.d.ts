import { ExpoGraphqlClient } from '../commandUtils/context/contextUtils/createGraphqlClient';
export declare function parseEnvironmentFlag(value: string | undefined): string[] | null;
/**
 * Resolve requested EAS environment names against what the project already uses.
 * Shared across integrations; pass integration-specific defaults and messages at each callsite.
 */
export declare function resolveTargetEnvironmentsAsync(graphqlClient: ExpoGraphqlClient, projectId: string, requested: string[], nonInteractive: boolean, { defaultEnvironments, cancelMessage, }: {
    defaultEnvironments: string[];
    cancelMessage: (knownEnvironments: string) => string;
}): Promise<string[]>;
