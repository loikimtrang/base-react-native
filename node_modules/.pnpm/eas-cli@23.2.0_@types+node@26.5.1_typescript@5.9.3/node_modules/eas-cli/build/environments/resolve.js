"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseEnvironmentFlag = parseEnvironmentFlag;
exports.resolveTargetEnvironmentsAsync = resolveTargetEnvironmentsAsync;
const prompts_1 = require("../prompts");
const variables_1 = require("./variables");
function parseEnvironmentFlag(value) {
    if (value === undefined) {
        return null;
    }
    if (!value.trim()) {
        throw new Error('Pass at least one EAS environment to --environment (e.g. --environment preview).');
    }
    const environments = [
        ...new Set(value
            .split(',')
            .map(part => part.trim().toLowerCase())
            .filter(Boolean)),
    ];
    if (environments.length === 0) {
        throw new Error('Pass at least one EAS environment to --environment (e.g. --environment preview).');
    }
    for (const environment of environments) {
        if (environment.length < 3 || environment.length > 100 || !/^[a-z0-9_-]+$/.test(environment)) {
            throw new Error(`Invalid EAS environment "${environment}". Use 3–100 lowercase letters, numbers, dashes, or underscores.`);
        }
    }
    return environments;
}
/**
 * Resolve requested EAS environment names against what the project already uses.
 * Shared across integrations; pass integration-specific defaults and messages at each callsite.
 */
async function resolveTargetEnvironmentsAsync(graphqlClient, projectId, requested, nonInteractive, { defaultEnvironments, cancelMessage, }) {
    let known = await (0, variables_1.getProjectEnvironmentVariableEnvironmentsAsync)(graphqlClient, projectId);
    if (known.length === 0) {
        known = [...defaultEnvironments];
    }
    const knownSet = new Set(known);
    const unknown = requested.filter(environment => !knownSet.has(environment));
    if (unknown.length === 0) {
        return requested;
    }
    const defaultEnvironmentSet = new Set(defaultEnvironments);
    const hasCustom = unknown.some(environment => !defaultEnvironmentSet.has(environment));
    if (nonInteractive) {
        const hint = hasCustom
            ? 'Custom environments require an Enterprise plan; pass only default environments, or upgrade.'
            : 'Re-run interactively to create them, or pass only existing environments.';
        throw new Error(`EAS environment(s) not found on this project: ${unknown.join(', ')}. Known: ${known.join(', ')}. ${hint}`);
    }
    const listed = unknown.map(environment => `"${environment}"`).join(', ');
    const isPlural = unknown.length > 1;
    const noun = isPlural ? 'environments' : 'environment';
    const verb = isPlural ? 'are' : 'is';
    const customHint = hasCustom
        ? ' Custom environments require an Enterprise plan; values will be written if your account supports them.'
        : '';
    const create = await (0, prompts_1.confirmAsync)({
        message: `EAS ${noun} ${listed} ${verb} not used on this project yet.${customHint} Continue provisioning?`,
    });
    if (!create) {
        throw new Error(cancelMessage(known.join(', ')));
    }
    // Custom environments are created lazily when env vars are written (createForAppAsync).
    return requested;
}
