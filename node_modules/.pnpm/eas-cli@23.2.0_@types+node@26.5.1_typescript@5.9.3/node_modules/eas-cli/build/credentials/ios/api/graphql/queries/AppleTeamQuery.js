"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AppleTeamQuery = void 0;
const tslib_1 = require("tslib");
const graphql_1 = require("graphql");
const graphql_tag_1 = tslib_1.__importDefault(require("graphql-tag"));
const client_1 = require("../../../../../graphql/client");
const AppleTeam_1 = require("../../../../../graphql/types/credentials/AppleTeam");
exports.AppleTeamQuery = {
    async getAllForAccountAsync(graphqlClient, { accountName, offset, limit, }) {
        // appleTeamsPaginated is cursor-based, so emulate offset/limit by fetching
        // pages from the start until enough nodes are collected
        const start = offset ?? 0;
        const teams = [];
        let after;
        while (limit == null || teams.length < start + limit) {
            const connection = await exports.AppleTeamQuery.getAllForAccountPaginatedAsync(graphqlClient, accountName, { first: 100, after });
            teams.push(...connection.edges.map(edge => edge.node));
            if (!connection.pageInfo.hasNextPage) {
                break;
            }
            after = connection.pageInfo.endCursor ?? undefined;
        }
        return limit == null ? teams.slice(start) : teams.slice(start, start + limit);
    },
    async getAllForAccountPaginatedAsync(graphqlClient, accountName, { after, first, before, last, filter, }) {
        const data = await (0, client_1.withErrorHandlingAsync)(graphqlClient
            .query((0, graphql_tag_1.default) `
            query AppleTeamsPaginatedByAccountQuery(
              $accountName: String!
              $after: String
              $first: Int
              $before: String
              $last: Int
              $filter: AppleTeamFilterInput
            ) {
              account {
                byName(accountName: $accountName) {
                  id
                  appleTeamsPaginated(
                    after: $after
                    first: $first
                    before: $before
                    last: $last
                    filter: $filter
                  ) {
                    edges {
                      cursor
                      node {
                        id
                        ...AppleTeamFragment
                      }
                    }
                    pageInfo {
                      hasNextPage
                      hasPreviousPage
                      startCursor
                      endCursor
                    }
                  }
                }
              }
            }
            ${(0, graphql_1.print)(AppleTeam_1.AppleTeamFragmentNode)}
          `, { accountName, after, first, before, last, filter }, {
            additionalTypenames: ['AppleTeam'],
        })
            .toPromise());
        return data.account.byName.appleTeamsPaginated;
    },
    async getByAppleTeamIdentifierAsync(graphqlClient, accountId, appleTeamIdentifier) {
        const data = await (0, client_1.withErrorHandlingAsync)(graphqlClient
            .query((0, graphql_tag_1.default) `
            query AppleTeamByIdentifierQuery($accountId: ID!, $appleTeamIdentifier: String!) {
              appleTeam {
                byAppleTeamIdentifier(accountId: $accountId, identifier: $appleTeamIdentifier) {
                  id
                  ...AppleTeamFragment
                }
              }
            }
            ${(0, graphql_1.print)(AppleTeam_1.AppleTeamFragmentNode)}
          `, {
            accountId,
            appleTeamIdentifier,
        }, {
            additionalTypenames: ['AppleTeam'],
        })
            .toPromise());
        return data.appleTeam.byAppleTeamIdentifier ?? null;
    },
};
