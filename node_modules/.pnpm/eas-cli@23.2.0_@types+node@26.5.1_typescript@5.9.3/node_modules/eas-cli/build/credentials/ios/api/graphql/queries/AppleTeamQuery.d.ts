import { ExpoGraphqlClient } from '../../../../../commandUtils/context/contextUtils/createGraphqlClient';
import { AppleTeamFilterInput, AppleTeamFragment } from '../../../../../graphql/generated';
import { Connection } from '../../../../../utils/relay';
export declare const AppleTeamQuery: {
    getAllForAccountAsync(graphqlClient: ExpoGraphqlClient, { accountName, offset, limit, }: {
        accountName: string;
        offset?: number | null;
        limit?: number | null;
    }): Promise<AppleTeamFragment[]>;
    getAllForAccountPaginatedAsync(graphqlClient: ExpoGraphqlClient, accountName: string, { after, first, before, last, filter, }: {
        after?: string;
        first?: number;
        before?: string;
        last?: number;
        filter?: AppleTeamFilterInput;
    }): Promise<Connection<AppleTeamFragment>>;
    getByAppleTeamIdentifierAsync(graphqlClient: ExpoGraphqlClient, accountId: string, appleTeamIdentifier: string): Promise<AppleTeamFragment | null>;
};
