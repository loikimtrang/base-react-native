import { Platform } from '@expo/eas-build-job';
import { SubmissionContext } from '../context';
/**
 * Best-effort TestFlight internal group setup for an App Store Connect app
 * that already exists with `ascAppId` provided.
 */
export declare function ensureTestFlightSetupForExistingAppAsync(ctx: SubmissionContext<Platform.IOS>, ascAppIdentifier: string): Promise<void>;
