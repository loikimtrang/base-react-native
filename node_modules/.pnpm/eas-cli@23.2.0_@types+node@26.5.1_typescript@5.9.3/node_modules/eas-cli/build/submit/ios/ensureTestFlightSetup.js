"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ensureTestFlightSetupForExistingAppAsync = ensureTestFlightSetupForExistingAppAsync;
const tslib_1 = require("tslib");
const apple_utils_1 = require("@expo/apple-utils");
const nullthrows_1 = tslib_1.__importDefault(require("nullthrows"));
const authenticateTypes_1 = require("../../credentials/ios/appstore/authenticateTypes");
const authenticate_1 = require("../../credentials/ios/appstore/authenticate");
const ensureTestFlightGroup_1 = require("../../credentials/ios/appstore/ensureTestFlightGroup");
const resolveCredentials_1 = require("../../credentials/ios/appstore/resolveCredentials");
const AscApiKeyUtils_1 = require("../../credentials/ios/actions/AscApiKeyUtils");
const log_1 = tslib_1.__importDefault(require("../../log"));
const bundleIdentifier_1 = require("../../project/ios/bundleIdentifier");
/**
 * Best-effort TestFlight internal group setup for an App Store Connect app
 * that already exists with `ascAppId` provided.
 */
async function ensureTestFlightSetupForExistingAppAsync(ctx, ascAppIdentifier) {
    if (!ctx.autoTestFlightSetup) {
        return;
    }
    try {
        const bundleIdentifier = ctx.applicationIdentifierOverride ??
            ctx.profile.bundleIdentifier ??
            (await (0, bundleIdentifier_1.getBundleIdentifierAsync)(ctx.projectDir, ctx.exp, ctx.vcsClient));
        const appLookupParams = {
            account: (0, nullthrows_1.default)(ctx.user.accounts.find(a => a.name === ctx.accountName), `You do not have access to account: ${ctx.accountName}`),
            projectName: ctx.projectName,
            bundleIdentifier,
        };
        if (!ctx.credentialsCtx.appStore.authCtx) {
            const teamType = (0, resolveCredentials_1.resolveAppleTeamTypeFromEnvironment)() ?? authenticateTypes_1.AppleTeamType.COMPANY_OR_ORGANIZATION;
            if ((0, resolveCredentials_1.hasAscEnvVars)()) {
                const teamId = process.env.EXPO_APPLE_TEAM_ID;
                if (!process.env.EXPO_ASC_API_KEY_PATH ||
                    !process.env.EXPO_ASC_KEY_ID ||
                    !process.env.EXPO_ASC_ISSUER_ID ||
                    !teamId) {
                    log_1.default.log('App Store Connect credentials are incomplete, skipping TestFlight setup');
                    return;
                }
                await ctx.credentialsCtx.appStore.ensureAuthenticatedAsync({
                    mode: authenticateTypes_1.AuthenticationMode.API_KEY,
                    teamId,
                    teamType,
                });
            }
            else {
                const resolvedKey = await (0, AscApiKeyUtils_1.resolveAscApiKeyForAppCredentialsAsync)({
                    graphqlClient: ctx.graphqlClient,
                    app: appLookupParams,
                });
                const teamId = resolvedKey?.teamId ?? process.env.EXPO_APPLE_TEAM_ID;
                if (!resolvedKey || !teamId) {
                    log_1.default.log('No complete App Store Connect credentials, skipping TestFlight setup');
                    return;
                }
                log_1.default.log('Using App Store Connect API Key from EAS credentials service.');
                await ctx.credentialsCtx.appStore.ensureAuthenticatedAsync({
                    mode: authenticateTypes_1.AuthenticationMode.API_KEY,
                    ascApiKey: resolvedKey.ascApiKey,
                    teamId,
                    teamName: resolvedKey.teamName,
                    teamType,
                });
            }
        }
        const authCtx = ctx.credentialsCtx.appStore.authCtx;
        if (!authCtx) {
            log_1.default.debug('No App Store Connect API key available, skipping TestFlight setup');
            return;
        }
        const app = await apple_utils_1.App.infoAsync((0, authenticate_1.getRequestContext)(authCtx), { id: ascAppIdentifier });
        await (0, ensureTestFlightGroup_1.ensureTestFlightGroupExistsAsync)(app, { nonInteractive: ctx.nonInteractive });
    }
    catch (error) {
        // Group setup is a convenience on top of the submission and must never
        // block it.
        log_1.default.warn('Skipping TestFlight group setup:', error);
    }
}
