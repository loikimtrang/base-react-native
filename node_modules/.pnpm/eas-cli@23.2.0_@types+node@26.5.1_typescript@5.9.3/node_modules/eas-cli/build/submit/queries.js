"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SUBMISSIONS_LIMIT = void 0;
exports.listAndRenderSubmissionsOnAppAsync = listAndRenderSubmissionsOnAppAsync;
exports.getLatestSubmissionAsync = getLatestSubmissionAsync;
exports.getRecentSubmissionsAsync = getRecentSubmissionsAsync;
exports.getRecentSubmissionsWithStatusesAsync = getRecentSubmissionsWithStatusesAsync;
exports.formatSubmissionChoice = formatSubmissionChoice;
const tslib_1 = require("tslib");
const chalk_1 = tslib_1.__importDefault(require("chalk"));
const formatSubmission_1 = require("./utils/formatSubmission");
const SubmissionQuery_1 = require("../graphql/queries/SubmissionQuery");
const log_1 = tslib_1.__importDefault(require("../log"));
const platform_1 = require("../platform");
const date_1 = require("../utils/date");
const json_1 = require("../utils/json");
const queries_1 = require("../utils/queries");
exports.SUBMISSIONS_LIMIT = 50;
async function listAndRenderSubmissionsOnAppAsync(graphqlClient, { projectId, projectDisplayName, filter, paginatedQueryOptions, }) {
    if (paginatedQueryOptions.nonInteractive) {
        const submissions = await SubmissionQuery_1.SubmissionQuery.allForAppAsync(graphqlClient, projectId, {
            limit: paginatedQueryOptions.limit ?? exports.SUBMISSIONS_LIMIT,
            offset: paginatedQueryOptions.offset,
            ...filter,
        });
        renderPageOfSubmissions({ submissions, projectDisplayName, paginatedQueryOptions });
    }
    else {
        await (0, queries_1.paginatedQueryWithConfirmPromptAsync)({
            limit: paginatedQueryOptions.limit ?? exports.SUBMISSIONS_LIMIT,
            offset: paginatedQueryOptions.offset,
            queryToPerform: (limit, offset) => SubmissionQuery_1.SubmissionQuery.allForAppAsync(graphqlClient, projectId, {
                limit,
                offset,
                ...filter,
            }),
            promptOptions: {
                title: 'Load more submissions?',
                renderListItems: submissions => {
                    renderPageOfSubmissions({ submissions, projectDisplayName, paginatedQueryOptions });
                },
            },
        });
    }
}
async function getLatestSubmissionAsync(graphqlClient, { projectId, filter, }) {
    const submissions = await SubmissionQuery_1.SubmissionQuery.allForAppAsync(graphqlClient, projectId, {
        limit: 1,
        offset: 0,
        ...filter,
    });
    return submissions[0] ?? null;
}
async function getRecentSubmissionsAsync(graphqlClient, { projectId, filter, limit = exports.SUBMISSIONS_LIMIT, }) {
    return await SubmissionQuery_1.SubmissionQuery.allForAppAsync(graphqlClient, projectId, {
        limit,
        offset: 0,
        ...filter,
    });
}
/**
 * Fetch a page per status and merge, newest first. The GraphQL filter takes a single status;
 * fetching one unfiltered page instead would let recent finished submissions push older
 * still-active ones off the page.
 */
async function getRecentSubmissionsWithStatusesAsync(graphqlClient, { projectId, platform, statuses, }) {
    const pages = await Promise.all(statuses.map(status => SubmissionQuery_1.SubmissionQuery.allForAppAsync(graphqlClient, projectId, {
        limit: exports.SUBMISSIONS_LIMIT,
        offset: 0,
        platform,
        status,
    })));
    return pages
        .flat()
        .sort((a, b) => new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime());
}
function formatSubmissionChoice(submission) {
    const platform = platform_1.appPlatformDisplayNames[submission.platform];
    const status = submission.status.toLowerCase().replace(/_/g, ' ');
    return `${platform} submission ${submission.id} (${status}, created ${(0, date_1.fromNow)(new Date(submission.createdAt))} ago)`;
}
function renderPageOfSubmissions({ submissions, projectDisplayName, paginatedQueryOptions, }) {
    if (paginatedQueryOptions.json) {
        (0, json_1.printJsonOnlyOutput)(submissions);
    }
    else {
        const list = submissions
            .map(submission => (0, formatSubmission_1.formatGraphQLSubmission)(submission))
            .join(`\n\n${chalk_1.default.dim('———')}\n\n`);
        log_1.default.addNewLineIfNone();
        log_1.default.log(chalk_1.default.bold(`Submissions for ${projectDisplayName}:`));
        log_1.default.log(`\n${list}`);
    }
}
