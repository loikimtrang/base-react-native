import { ExpoGraphqlClient } from '../commandUtils/context/contextUtils/createGraphqlClient';
import { PaginatedQueryOptions } from '../commandUtils/pagination';
import { AppPlatform, SubmissionStatus, SubmissionWithSubmittedBuildFragment } from '../graphql/generated';
export declare const SUBMISSIONS_LIMIT = 50;
export type SubmissionFilter = {
    platform?: AppPlatform;
    status?: SubmissionStatus;
};
export declare function listAndRenderSubmissionsOnAppAsync(graphqlClient: ExpoGraphqlClient, { projectId, projectDisplayName, filter, paginatedQueryOptions, }: {
    projectId: string;
    projectDisplayName: string;
    filter?: SubmissionFilter;
    paginatedQueryOptions: PaginatedQueryOptions;
}): Promise<void>;
export declare function getLatestSubmissionAsync(graphqlClient: ExpoGraphqlClient, { projectId, filter, }: {
    projectId: string;
    filter?: SubmissionFilter;
}): Promise<SubmissionWithSubmittedBuildFragment | null>;
export declare function getRecentSubmissionsAsync(graphqlClient: ExpoGraphqlClient, { projectId, filter, limit, }: {
    projectId: string;
    filter?: SubmissionFilter;
    limit?: number;
}): Promise<SubmissionWithSubmittedBuildFragment[]>;
/**
 * Fetch a page per status and merge, newest first. The GraphQL filter takes a single status;
 * fetching one unfiltered page instead would let recent finished submissions push older
 * still-active ones off the page.
 */
export declare function getRecentSubmissionsWithStatusesAsync(graphqlClient: ExpoGraphqlClient, { projectId, platform, statuses, }: {
    projectId: string;
    platform?: AppPlatform;
    statuses: SubmissionStatus[];
}): Promise<SubmissionWithSubmittedBuildFragment[]>;
export declare function formatSubmissionChoice(submission: SubmissionWithSubmittedBuildFragment): string;
