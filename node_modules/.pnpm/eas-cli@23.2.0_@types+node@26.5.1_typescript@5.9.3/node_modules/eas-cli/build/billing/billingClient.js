"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.BillingClient = void 0;
const api_1 = require("../api");
class BillingClient {
    apiV2Client;
    constructor(authInfo) {
        this.apiV2Client = new api_1.ApiV2Client(authInfo);
    }
    async createCheckoutSessionAsync(accountId, planType) {
        const { data } = await this.apiV2Client.postAsync('stripe-auth/checkout', {
            body: { accountId, planType },
        });
        return data;
    }
    async createCustomerPortalSessionAsync(accountId) {
        const { data } = await this.apiV2Client.postAsync('stripe-auth/customer-portal', {
            body: { accountId },
        });
        return data.customerPortal;
    }
}
exports.BillingClient = BillingClient;
