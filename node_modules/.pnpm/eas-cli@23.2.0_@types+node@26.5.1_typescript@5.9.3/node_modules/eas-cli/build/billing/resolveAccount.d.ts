import { ExpoGraphqlClient } from '../commandUtils/context/contextUtils/createGraphqlClient';
import { type AccountSubscriptionInfo } from '../graphql/queries/AccountQuery';
import { Actor } from '../user/User';
export type BillingAccount = {
    id: string;
    name: string;
    subscription: AccountSubscriptionInfo | null;
};
type SubscriptionFilter = 'subscribed' | 'unsubscribed';
export declare function resolveBillingAccountAsync({ graphqlClient, actor, accountName, nonInteractive, subscriptionFilter, }: {
    graphqlClient: ExpoGraphqlClient;
    actor: Actor;
    accountName: string | undefined;
    nonInteractive: boolean;
    subscriptionFilter: SubscriptionFilter;
}): Promise<BillingAccount>;
export {};
