import type { CheckoutPlanType } from './plans';
export type CheckoutSession = {
    id: string;
    clientSecret: string | null;
    url: string | null;
};
export type CustomerPortalSession = {
    url: string;
};
export declare class BillingClient {
    private readonly apiV2Client;
    constructor(authInfo: {
        accessToken: string | null;
        sessionSecret: string | null;
    });
    createCheckoutSessionAsync(accountId: string, planType: CheckoutPlanType): Promise<CheckoutSession>;
    createCustomerPortalSessionAsync(accountId: string): Promise<CustomerPortalSession>;
}
