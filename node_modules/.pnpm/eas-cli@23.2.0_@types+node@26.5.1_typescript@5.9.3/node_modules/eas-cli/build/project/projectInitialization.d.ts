import { ExpoGraphqlClient } from '../commandUtils/context/contextUtils/createGraphqlClient';
import { Actor } from '../user/User';
export type InitializeMethodOptions = {
    force: boolean;
    nonInteractive: boolean;
};
export type ProjectInitStatus = 'created' | 'linked' | 'already-linked';
export type ProjectInitResult = {
    projectId: string;
    status: ProjectInitStatus;
};
export declare function ensureOwnerSlugConsistencyAsync(graphqlClient: ExpoGraphqlClient, projectId: string, projectDir: string, { force, nonInteractive }: InitializeMethodOptions): Promise<{
    owner: string;
    slug: string;
}>;
export declare function initializeWithExplicitIDAsync(projectId: string, projectDir: string, { force, nonInteractive }: InitializeMethodOptions): Promise<ProjectInitStatus>;
export declare function initializeWithoutExplicitIDAsync(graphqlClient: ExpoGraphqlClient, actor: Actor, projectDir: string, { force, nonInteractive, accountName: accountNameArgument, }: InitializeMethodOptions & {
    accountName?: string;
}): Promise<ProjectInitResult>;
