import { App } from '@expo/apple-utils';
import { ExpoConfig } from '@expo/config';
import { Analytics } from '../analytics/AnalyticsManager';
import { ExpoGraphqlClient } from '../commandUtils/context/contextUtils/createGraphqlClient';
import { Actor } from '../user/User';
import { Client } from '../vcs/vcs';
/** Resolve the App Store Connect app that TestFlight feedback and crashes should be read from. */
export declare function resolveTestFlightAppAsync({ actor, analytics, exp, graphqlClient, nonInteractive, profileName, projectDir, projectId, vcsClient, }: {
    actor: Actor;
    analytics: Analytics;
    exp: ExpoConfig;
    graphqlClient: ExpoGraphqlClient;
    nonInteractive: boolean;
    profileName?: string;
    projectDir: string;
    projectId: string;
    vcsClient: Client;
}): Promise<App>;
