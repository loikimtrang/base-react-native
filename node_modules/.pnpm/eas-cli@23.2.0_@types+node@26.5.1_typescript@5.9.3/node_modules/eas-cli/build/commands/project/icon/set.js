"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const core_1 = require("@oclif/core");
const chalk_1 = tslib_1.__importDefault(require("chalk"));
const url_1 = require("../../../build/utils/url");
const EasCommand_1 = tslib_1.__importDefault(require("../../../commandUtils/EasCommand"));
const flags_1 = require("../../../commandUtils/flags");
const AppQuery_1 = require("../../../graphql/queries/AppQuery");
const log_1 = tslib_1.__importStar(require("../../../log"));
const ora_1 = require("../../../ora");
const projectIcon_1 = require("../../../project/projectIcon");
class ProjectIconSet extends EasCommand_1.default {
    static description = 'set the project icon displayed on the EAS dashboard';
    static args = {
        path: core_1.Args.string({
            required: true,
            description: 'Path to the icon image (PNG or JPEG, at most 10 MB). Non-square images are center-cropped to a square.',
        }),
    };
    static flags = {
        ...flags_1.EASNonInteractiveFlag,
    };
    static contextDefinition = {
        ...this.ContextOptions.ProjectId,
        ...this.ContextOptions.LoggedIn,
    };
    async runAsync() {
        const { args: { path: imagePath }, flags, } = await this.parse(ProjectIconSet);
        const { projectId, loggedIn: { graphqlClient }, } = await this.getContextAsync(ProjectIconSet, {
            nonInteractive: flags['non-interactive'],
        });
        await (0, projectIcon_1.validateIconAsync)(imagePath);
        const app = await AppQuery_1.AppQuery.byIdAsync(graphqlClient, projectId);
        const projectDashboardUrl = (0, url_1.getProjectDashboardUrl)(app.ownerAccount.name, app.slug);
        const previousProfileImageUrl = await AppQuery_1.AppQuery.byIdProfileImageUrlAsync(graphqlClient, projectId);
        const spinner = (0, ora_1.ora)('Uploading project icon').start();
        try {
            await (0, projectIcon_1.uploadProjectIconAsync)(graphqlClient, { projectId, imagePath });
            // The icon is processed asynchronously (resized and assigned to the
            // project by the server), so poll until the icon URL changes.
            spinner.text = 'Processing project icon';
            await (0, projectIcon_1.pollForProfileImageChangeAsync)(graphqlClient, {
                projectId,
                previousProfileImageUrl,
                projectDashboardUrl,
            });
            spinner.succeed(`Set icon for ${chalk_1.default.bold(app.fullName)}`);
            log_1.default.withTick(`View it on the project page: ${(0, log_1.link)(projectDashboardUrl)}`);
        }
        catch (error) {
            spinner.fail('Failed to set project icon');
            throw error;
        }
    }
}
exports.default = ProjectIconSet;
