"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EAS_OBSERVE_FEATURE_NOT_AVAILABLE_IN_FREE_TIER_ERROR_CODE = exports.EAS_OBSERVE_PLAN_UPGRADE_REQUIRED_ERROR_CODE = void 0;
exports.isObservePlanGateError = isObservePlanGateError;
exports.withObservePlanGateHandlingAsync = withObservePlanGateHandlingAsync;
const errors_1 = require("../commandUtils/errors");
const client_1 = require("../graphql/client");
// Must match the server's Observe plan-gate error codes (`ExpoErrorCode`). Both
// signal "this Observe feature is not available on the account's current plan";
// they differ only in which plans are blocked, which is decided server-side.
exports.EAS_OBSERVE_PLAN_UPGRADE_REQUIRED_ERROR_CODE = 'EAS_OBSERVE_PLAN_UPGRADE_REQUIRED';
exports.EAS_OBSERVE_FEATURE_NOT_AVAILABLE_IN_FREE_TIER_ERROR_CODE = 'EAS_OBSERVE_FEATURE_NOT_AVAILABLE_IN_FREE_TIER';
const OBSERVE_PLAN_GATE_ERROR_CODES = new Set([
    exports.EAS_OBSERVE_PLAN_UPGRADE_REQUIRED_ERROR_CODE,
    exports.EAS_OBSERVE_FEATURE_NOT_AVAILABLE_IN_FREE_TIER_ERROR_CODE,
]);
function findObservePlanGateMessage(error) {
    if (!(error instanceof client_1.GraphqlError)) {
        return undefined;
    }
    return error.graphQLErrors.find(e => OBSERVE_PLAN_GATE_ERROR_CODES.has(e?.extensions?.errorCode))?.message;
}
/**
 * Whether an error is a server Observe plan-gate rejection. Useful where a
 * plan-gate error must escape error handling that would otherwise swallow it
 * (e.g. per-platform fetch loops that treat failures as empty results).
 */
function isObservePlanGateError(error) {
    return findObservePlanGateMessage(error) !== undefined;
}
/**
 * Runs an Observe GraphQL operation and, when the server rejects it because the
 * feature is not included in the account's plan, rethrows the server's upgrade
 * message (which links to the account's billing page) as an `EasCommandError`.
 * Reusable across Observe commands.
 */
async function withObservePlanGateHandlingAsync(fn) {
    try {
        return await fn();
    }
    catch (error) {
        const planGateMessage = findObservePlanGateMessage(error);
        if (planGateMessage !== undefined) {
            throw new errors_1.EasCommandError(planGateMessage);
        }
        throw error;
    }
}
