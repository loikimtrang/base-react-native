"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DeviceRunSessionEventZ = void 0;
exports.parseDeviceRunSessionEvents = parseDeviceRunSessionEvents;
const zod_1 = require("zod");
/**
 * Canonical record format for the activity events emitted during a remote device run (simulator)
 * session. Producers (e.g. the worker and agent-device) write these as NDJSON to the
 * session-events artifact and consumers (e.g. EAS CLI and the website) read them back. Keep this
 * schema in sync across every producer and consumer.
 */
exports.DeviceRunSessionEventZ = zod_1.z
    .object({
    v: zod_1.z.literal(1),
    eventId: zod_1.z.string(),
    ts: zod_1.z.string().refine(ts => !Number.isNaN(new Date(ts).getTime())),
    producer: zod_1.z.string(),
    type: zod_1.z.string(),
    operationId: zod_1.z.string().optional(),
    outcome: zod_1.z.enum(['success', 'failure']).optional(),
    durationMs: zod_1.z.number().optional(),
    summary: zod_1.z.string(),
    data: zod_1.z.record(zod_1.z.string(), zod_1.z.unknown()).optional(),
})
    .passthrough();
/**
 * Parse the NDJSON session-events log into canonical event records, de-duplicating by `eventId`
 * and sorting chronologically by `ts`. Malformed or incomplete lines (including records with
 * invalid timestamps) are ignored so that a partially-flushed log is still readable.
 */
function parseDeviceRunSessionEvents(eventLog) {
    const events = new Map();
    for (const line of eventLog.split('\n')) {
        try {
            const result = exports.DeviceRunSessionEventZ.safeParse(JSON.parse(line));
            if (result.success) {
                events.set(result.data.eventId, result.data);
            }
        }
        catch {
            // Ignore malformed or incomplete records.
        }
    }
    return [...events.values()].sort((left, right) => new Date(left.ts).getTime() - new Date(right.ts).getTime());
}
