"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CompositeFunctionConfigZ = void 0;
/**
 * Schema for local composite functions, reusable step groups referenced via `uses:` in EAS
 * workflows (`.eas/workflows/*.yml`) or inline job step definitions.
 *
 * This module defines the shape of a composite function configuration file (`function.yml`).
 * Callers that load composite function files format validation errors from `CompositeFunctionConfigZ`.
 * Local composite functions are not supported in `.eas/build/*.yml` custom build config files.
 */
const zod_1 = require("zod");
const step_1 = require("./step");
const CompositeFunctionInputValueTypeNameZ = zod_1.z.enum(['string', 'boolean', 'number', 'json']);
const CompositeFunctionInputValueZ = zod_1.z.union([
    zod_1.z.string(),
    zod_1.z.boolean(),
    zod_1.z.number(),
    zod_1.z.array(zod_1.z.unknown()),
    zod_1.z.record(zod_1.z.string(), zod_1.z.unknown()),
]);
const CompositeFunctionInputZ = zod_1.z.union([
    zod_1.z
        .string()
        .describe('Shorthand for an input name with default type "string" and no default value.'),
    zod_1.z
        .object({
        name: zod_1.z.string(),
        type: CompositeFunctionInputValueTypeNameZ.default('string'),
        default_value: CompositeFunctionInputValueZ.optional(),
        allowed_values: zod_1.z.array(CompositeFunctionInputValueZ).optional(),
        required: zod_1.z.boolean().optional(),
    })
        .strict(),
]);
const CompositeFunctionOutputZ = zod_1.z
    .object({
    description: zod_1.z.string().optional(),
    /**
     * @example
     * value: '${{ steps.read.outputs.version }}'
     */
    value: zod_1.z.string().describe('Expression that resolves to the output value.'),
})
    .strict();
exports.CompositeFunctionConfigZ = zod_1.z
    .object({
    /**
     * @example
     * name: Setup
     */
    name: zod_1.z.string().optional().describe('Display name of the composite function.'),
    description: zod_1.z.string().optional(),
    /**
     * @example
     * inputs:
     *   - greeting
     *   - name: platform
     *     type: string
     *     default_value: ios
     */
    inputs: zod_1.z
        .array(CompositeFunctionInputZ)
        .optional()
        .describe('Inputs accepted by the composite function.'),
    /**
     * @example
     * outputs:
     *   version:
     *     value: '${{ steps.read.outputs.version }}'
     */
    outputs: zod_1.z
        .record(zod_1.z.string(), CompositeFunctionOutputZ)
        .optional()
        .describe('Named outputs exposed by the composite function to its caller.'),
    runs: zod_1.z.object({
        /**
         * @example
         * runs:
         *   steps:
         *     - id: read
         *       run: set-output version "1.0.0"
         */
        steps: zod_1.z
            .array(step_1.StepZ)
            .min(1, {
            message: 'A composite function must declare at least one step under "runs.steps".',
        })
            .describe('Steps executed when the composite function is invoked.'),
    }),
})
    .strict();
